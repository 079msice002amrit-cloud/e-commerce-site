name: Build and Deploy backend app

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    # Checkout the repository
    - name: Checkout repository
      uses: actions/checkout@v3

    # Log in to DockerHub
    - name: Log in to dockerhub
      run: |
        echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login --username "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
      
    # Build and Push the Docker image
    - name: Build and Push Docker Image
      run: |
        BRANCH_NAME=${{ github.ref_name }}
        docker build -t ecomapp .
        docker tag ecomapp:latest ${{ secrets.DOCKERHUB_USERNAME }}/ecomapp:$BRANCH_NAME
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/ecomapp:$BRANCH_NAME

    # # Deploy on AWS EC2
    # - name: Deploy on AWS EC2
    #   uses: appleboy/ssh-action@v0.1.6
    #   with:
    #     host: ${{ secrets.ANSIBLE_IP }}
    #     username: ubuntu
    #     key: ${{ secrets.SSH_KEY }}
    #     options: "-o StrictHostKeyChecking=no"
    #     script: |
    #       cd /home/ubuntu/ansible
    #       ansible-playbook deploy.yml --become